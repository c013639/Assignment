# key variable for refrencing 
variable "key_name" {
  default = "ec2Key"      # if we keep default blank it will ask for a value when we execute terraform apply
}

# base_path for refrencing 
variable "base_path" {
  default = "C:\\Users\\A3285"
}